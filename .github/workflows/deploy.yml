name: deploy

on:
    push:
        branches:
            - master

jobs:
    deploy:
        # https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#choosing-github-hosted-runners
        runs-on: ubuntu-20.04
        steps:
            # https://github.com/marketplace/actions/checkout
            - uses: actions/checkout@v3.0.2
            # https://github.com/marketplace/actions/setup-node-js-environment
            - uses: actions/setup-node@v3.1.1
              with:
                  node-version: 18
            # https://github.com/marketplace/actions/cache
            - uses: actions/cache@v3.0.2
              with:
                  path: ~/.local/share/pnpm/store
                  key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
                  restore-keys: ${{ runner.os }}-pnpm-
            - run: |
                  # https://www.npmjs.com/package/@cloudflare/wrangler
                  npm install -g pnpm@7
                  pnpm install
                  pnpm add wrangler@2.0.1
                  pnpm store prune

            - run: make test

            - run: make src/feedbox
            - working-directory: src/feedbox
              env:
                  CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
                  CF_ZONE_ID: ${{ secrets.CF_ZONE_ID }}
                  CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
              run: |
                  pnpm exec wrangler publish

            - run: make src/current-ip
            - working-directory: src/current-ip
              env:
                  CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
                  CF_ZONE_ID: ${{ secrets.CF_ZONE_ID }}
                  CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
              run: |
                  echo '${{ secrets.ROLLBAR_KEY }}' | pnpm exec wrangler secret put 'ROLLBAR_KEY'
                  echo '${{ secrets.ROLLBAR_TG_BOT_TOKEN }}' | pnpm exec wrangler secret put 'ROLLBAR_TG_BOT_TOKEN'
                  echo '${{ secrets.ROLLBAR_TG_CHAT_ID }}' | pnpm exec wrangler secret put 'ROLLBAR_TG_CHAT_ID'
                  echo '${{ secrets.IP_PASS_NUC }}' | pnpm exec wrangler secret put 'IP_PASS_NUC'
                  pnpm exec wrangler publish

            - run: make src/bcc
            - working-directory: src/bcc
              env:
                  CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
                  CF_ZONE_ID: ${{ secrets.CF_ZONE_ID }}
                  CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
              run: |
                  echo '${{ secrets.ROLLBAR_KEY }}' | pnpm exec wrangler secret put 'ROLLBAR_KEY'
                  echo '${{ secrets.DB_API }}' | pnpm exec wrangler secret put 'DB_API'
                  echo '${{ secrets.DB_TOKEN }}' | pnpm exec wrangler secret put 'DB_TOKEN'
                  echo '${{ secrets.BCC_WEBHOOK_PATH }}' | pnpm exec wrangler secret put 'BCC_WEBHOOK_PATH'
                  echo '${{ secrets.BCC_BOT_TOKEN }}' | pnpm exec wrangler secret put 'BCC_BOT_TOKEN'
                  pnpm exec wrangler publish
                  curl -s -f 'https://api.telegram.org/bot${{ secrets.BCC_BOT_TOKEN }}/setWebhook?url=https%3A%2F%2Fworker.h11.io%2Ftelegram%2Fbcc%2F${{ secrets.BCC_WEBHOOK_PATH }}'

            - run: make src/rollbar
            - working-directory: src/rollbar
              env:
                  CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
                  CF_ZONE_ID: ${{ secrets.CF_ZONE_ID }}
                  CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
              run: |
                  echo '${{ secrets.ROLLBAR_TG_BOT_TOKEN }}' | pnpm exec wrangler secret put 'ROLLBAR_TG_BOT_TOKEN'
                  echo '${{ secrets.ROLLBAR_TG_CHAT_ID }}' | pnpm exec wrangler secret put 'ROLLBAR_TG_CHAT_ID'
                  pnpm exec wrangler publish

            - run: make src/backup
            - working-directory: src/backup
              env:
                  CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
                  CF_ZONE_ID: ${{ secrets.CF_ZONE_ID }}
                  CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
              run: |
                  echo '${{ secrets.ROLLBAR_KEY }}' | pnpm exec wrangler secret put 'ROLLBAR_KEY'
                  echo '${{ secrets.BACKUP_PASS_BEANCOUNT }}' | pnpm exec wrangler secret put 'BACKUP_PASS_BEANCOUNT'
                  pnpm exec wrangler publish

            - run: make src/backup-heroku-pg
            - working-directory: src/backup-heroku-pg
              env:
                  CF_ACCOUNT_ID: ${{ secrets.CF_ACCOUNT_ID }}
                  CF_ZONE_ID: ${{ secrets.CF_ZONE_ID }}
                  CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
              run: |
                  echo '${{ secrets.ROLLBAR_KEY }}' | pnpm exec wrangler secret put 'ROLLBAR_KEY'
                  echo '${{ secrets.BACKUP_HEROKU_PG_APP }}' | pnpm exec wrangler secret put 'BACKUP_HEROKU_PG_APP'
                  echo '${{ secrets.BACKUP_HEROKU_PG_TOKEN }}' | pnpm exec wrangler secret put 'BACKUP_HEROKU_PG_TOKEN'
                  pnpm exec wrangler publish
